# urwort Deploy Configuration
#
# This file is read by the GitHub Actions deploy-server.yml workflow.
# Update SERVER_IP here when the Hetzner server changes — no GitHub
# Secrets needed.
#
# The SSH deploy key lives in infra/deploy-keys/urwort_ed25519 (committed
# to this private repo).

SERVER_IP=
